# تقرير مقارنة: DriveHub — الخطة مقابل التنفيذ الحالي

**مصدر الخطة:** `DriveHub_Project_Plan.pdf` / `.docx` — **النسخة 3.3** (متطابقة مع `docs/generate_plan_docx.py` و`docs/new-updates.md`)  
**تاريخ المراجعة:** 30 يونيو 2026  
**نطاق المراجعة:** Backend (Node/Express/MongoDB) + Frontend (React/Vite/Tailwind RTL)

---

## ملخص تنفيذي

المشروع **تجاوز مرحلة الهيكل الأولي** بشكل واضح: معظم نماذج البيانات والمسارات والبوابات السبع موجودة وتعمل منطقياً. التقدير الإجمالي:


| المحور                             | نسبة تقريبية |
| ---------------------------------- | ------------ |
| البنية التحتية (Auth, RBAC, نماذج) | ~95%         |
| الصفحات العامة والزائر             | ~85%         |
| الدورات والاشتراك والدفع           | ~80%         |
| التعليم والاختبارات التجريبية      | ~65%         |
| المرور والنتائج والشهادات          | ~80%         |
| المحاكي 3D والتفاعل المتقدم        | ~0%          |
| الدفع الحقيقي والإشعارات الخارجية  | ~25%         |
| النشر والإنتاج                     | غير مكتمل    |


---



## 1. ما تم تنفيذه بالكامل ✅



### 1.1 البنية التحتية والأمان


| المتطلب (الخطة)               | الحالة                                                  |
| ----------------------------- | ------------------------------------------------------- |
| React + Vite + Tailwind + RTL | ✅ منفّذ                                                 |
| Node.js + Express + MongoDB   | ✅ منفّذ                                                 |
| JWT + RBAC لـ 7 أدوار         | ✅ `auth.service`, `rolePermissions`, حراس Frontend      |
| أدوار متعددة + تبديل سياق     | ✅ `/auth/contexts`, `switch-context`, `RoleSwitcher`    |
| ممنوع الحذف — إيقاف/حظر فقط   | ✅ `suspendUser`, `suspendSchool`؛ حذف المدرسة بشروط فقط |
| تشفير مستندات الهوية/الرخص    | ✅ AES في `document.service` + `DocumentAccessLog`       |
| Audit Log                     | ✅ نموذج + middleware + صفحة Admin                       |
| Validation (Zod/Joi)          | ✅ validators على الخادم + Zod في الواجهة                |




### 1.2 الأدوار والبوابات


| الدور  | Backend               | Frontend                                             |
| ------ | --------------------- | ---------------------------------------------------- |
| زائر   | رخص، مدارس، عينة، FAQ | `HomePage`, `LicensesPage`, `SamplePage`, `FaqPage`… |
| مسجّل  | ملف، اشتراك، إشعارات  | `ProfilePage`, `EnrollPage`, `NotificationsPage`     |
| طالب   | `/student/*` كامل     | 7 صفحات في `StudentRoutes`                           |
| مدرب   | `/coach/*`            | جدول، طلاب، ملاحظات، تقييم دروس                      |
| مدير   | `/manager/*`          | دورات، طلبات، مدربون، بنوك أسئلة، قوائم…             |
| Admin  | `/admin/*`            | مدارس، تسعير، مستخدمون، تقارير، إعلانات…             |
| المرور | `/traffic/*`          | قوائم، مواعيد، نتائج                                 |




### 1.3 قواعد العمل الأساسية (من القسم 0 و7 في الخطة)


| القاعدة                                   | التنفيذ                                      |
| ----------------------------------------- | -------------------------------------------- |
| مدة الدورة 15 يوم                         | ✅ `course.helper.computeTrainingEnd`         |
| إطلاق ممنوع قبل 15 يوم من السابقة         | ✅ `courseHelper.canLaunchNewCourse`          |
| حجز المكان عند الدفع فقط                  | ✅ `payment.service.payFromWallet` + تأكيد يدوي (احتياطي) |
| طلب معلّق واحد                            | ✅ `assertNoPending`                          |
| فلترة طلبات بالطابع الزمني + الأماكن      | ✅ `filterRequestsForManager` + `WaitingList` |
| إغلاق تلقائي عند اكتمال الأماكن بالدفع    | ✅ في `payment.service`                       |
| قائمة انتظار ذكية                         | ✅ نموذج + ترقية عند تحرير مكان               |
| فئات منفصلة — لا دمج                      | ✅ تحقق في `enrollment.service`               |
| ب1/ب2 — اختيار عند التسجيل                | ✅ `LicenseSubType` + إلزام B1/B2             |
| بيانات الرخص من Admin                     | ✅ `AdminLicensesPage`, `license.service`      |
| دفع إعادة الاشتراك 3×50% ثم 100%          | ✅ `retakePayment.js` + `payment.service`     |
| أرشيف عند إعادة الاشتراك                  | ✅ `EnrollmentArchive`                        |
| نطاق إعادة (شقين / عملي فقط)              | ✅ `deriveRetakeScope`                        |
| عمولة 2% افتراضياً                        | ✅ `payment.helper.splitPayment`              |
| مدة الدرس العملي ساعة                     | ✅ `durationMinutes = 60`                     |
| امتحان نهائي خارج المنصة — تنسيق فقط      | ✅ `TrafficExamSchedule` + عرض للطالب         |
| مرحلتان: نظري ثم عملي                     | ✅ `trafficResult.helper`, `FinalExamResult`  |
| رسب نظري → راسب نهائي                     | ✅                                            |
| ناجح نظري + رسب عملي → إعادة عملي (مرتان) | ✅                                            |
| رفع أسماء → Admin → المرور                | ✅ `roster.service` + `distributeRosters`     |
| QR للتحقق من قوائم الطلاب                 | ✅ token + صفحة تحقق Admin                    |
| عينة: زائر جزئي / مسجّل كامل              | ✅ `content.service.getSample` + `SamplePage` |




### 1.4 الوظائف حسب الدور

**الزائر والعام**

- بطاقات رخص + تفاصيل (بدون أسعار في العرض العام) ✅
- أقرب مدارس بترتيب المسافة + خريطة تفاعلية (Leaflet) ✅
- فلاتر: فئة الرخصة، مدربات سيدات ✅
- متطلبات التسجيل، FAQ accordion (من API مع fallback) ✅
- طلب إضافة مدرسة ✅
- تقييمات المدارس (بموافقة Admin) ✅

**المسجّل والطالب**

- تدفق اشتراك كامل (مدرسة → دورة → تأكيد) ✅
- إلغاء الطلب المعلّق ✅
- مهلة دفع + تذكير (cron) ✅
- حجز مسبق `pre-registration` ✅
- محتوى نظري + مشترك + مخصص + فيديوهات ✅
- وضع فتح متدرج/كامل ✅
- اختبارات تجريبية: مؤقت، عشوائية، حلول بعد الانتهاء ✅
- إعادة تجريبي عند الرسوب فقط ✅
- حجز مواعيد يدوي + تلقائي ✅
- خيار مدربة للسيدات ✅
- موعد امتحان المرور + شهادات + أرشيف ✅

**المدرب**

- إنشاء حساب من المدير + فئات اختصاص ✅
- جدول يومي ✅
- ملاحظات وتقييم دروس ✅
- طلب تعديل أسئلة/محتوى → موافقة المدير ✅

**مدير المدرسة**

- إنشاء/إغلاق/إطلاق دورة ✅
- قبول/رفض طلبات + سبب الرفض ✅
- إدارة مدربين وبنوك أسئلة ✅
- مراجعة تعديلات المحتوى ✅
- رفع قوائم أسماء ✅

**Admin**

- أسعار + عمولة + رخص ✅
- مدارس + طلبات اعتماد ✅
- مستخدمون + إيقاف ✅
- توزيع قوائم للمرور ✅
- تقارير + تحقق QR ✅
- إعلانات (CRUD) ✅
- إيقاف تسجيل على مستوى المنصة ✅

**المرور**

- استلام قوائم موزّعة ✅
- جدولة امتحانات ✅
- إدخال نتائج + تسجيل رخصة ✅



### 1.5 نماذج البيانات

معظم الكيانات في القسم 7 من الخطة موجودة (42 نموذجاً): `User`, `UserRole`, `Enrollment`, `TrainingCourse`, `WaitingList`, `QuestionBank`, `PracticeExam`, `TrafficExamSchedule`, `DrivingLicenseRecord`, `EnrollmentArchive`, `StudentRoster`, وغيرها.

---



## 2. ما تم تنفيذه جزئياً ⚠️


| الميزة                     | ما يعمل                                              | ما ينقص                                             |
| -------------------------- | ---------------------------------------------------- | --------------------------------------------------- |
| **الدفع**                  | دفع يدوي خارج المنصة؛ حساب المبلغ وتقسيم مدرسة/منصة؛ تأكيد من المدير/Admin | لا بوابة دفع إلكترونية — بالتصميم |
| **الإشعارات**              | داخل التطبيق + بريد (SMTP أو console في dev)          | SMS معطّل حالياً                                    |
| **البحث الجغرافي**         | GPS + يدوي + مسافة + خريطة                           | Google Maps / Mapbox عبر env؛ Leaflet احتياطي       |
| **المحتوى التفاعلي**       | عرض MCQ/T-F + seed                                   | محرر MVP للمدير (`/manager/content` + بنوك الأسئلة) |
| **الفتح المتدرج**          | واجهة + حفظ الوضع + فرض على API                      | —                                                   |
| **الحجز التلقائي للمدرب** | بحث فتحات + تخصص + تفضيل مدربة + أول موعد متاح | — |
| **الاختبار التجريبي**      | مؤقت + مراجعة + عتبة 60%                            | —                                                   |
| **QR ومكافحة التزوير**     | قوائم + إحصائيات + شهادات + `/verify/*` | — |
| **أمان المستندات (2.9)**   | AES + magic bytes + RBAC + DocumentAccessLog + HTTPS prod | — |
| **الإعلانات**              | Admin CRUD + عرض الرئيسية/بوابة الطالب               | —                                                   |
| **إحصائيات الطالب**        | صفحة `/student/statistics`                           | —                                                   |
| **الشهادات**               | مسار `/student/certificates`                         | —                                                   |
| **نتائج الامتحان**         | المرور فقط                                           | —                                                   |
| **حذف المدرسة**            | Admin بشروط (لا دورة نشطة)                           | —                                                   |
| **الاختبارات**             | unit + integration محدودة                            | لا تغطية شاملة لكل التدفقات                         |
| **وثيقة API**              | `API-ROUTES.md`                                      | تشير لـ stubs 501 — **قديمة**؛ الكود فعلياً منفّذ   |
| **البحث في الهيدر**        | مُنفَّذ حسب NOTES                                    | قد يحتاج تحسينات UX                                 |


---



## 3. ما لم يُنفَّذ بعد ❌


| #   | المتطلب (الخطة)                                        | الملاحظة                                     |
| --- | ------------------------------------------------------ | -------------------------------------------- |
| 1   | **محاكي القيادة 3D** (Three.js/Babylon + صوت + ≥30fps) | لا كود، لا نموذج `SimulatorSession`، لا صفحة |
| 2   | **مراقبة أداء المحاكي للمدرب**                         | مرتبط بغياب المحاكي                          |
| 3   | **بوابة دفع إنتاجية** (PayTabs/Telr + split تلقائي)    | غير مطلوب — الدفع يدوي خارج المنصة بالتصميم          |
| 4   | **SendGrid + Twilio إنتاجي**                           | بنية جاهزة، غير مفعّلة                       |
| 5   | **QR على كل التقارير/الإحصائيات**                      | ✅ إحصائيات + شهادات + قوائم + صفحات `/verify/*` |
| 6   | **محرر محتوى تفاعلي** لإنشاء مقالات + أسئلة            | يعتمد على `contentSeed.js`                   |
| 7   | **صفحة محاكي 3D** في Sitemap                           | غير موجودة                                   |
| 8   | **صفحة إحصائيات طالب مستقلة**                          | مدمجة في Dashboard                           |
| 9   | **اختبارات أداء** (≤3ث، 500+ مستخدم)                   | غير منفّذة                                   |
| 10  | **نشر Production** (المرحلة 5 من خارطة الطريق)         | غير مكتمل                                    |
| 11  | **عرض الإعلانات** في الواجهات العامة                   | CRUD فقط                                     |
| 12  | **تكامل خرائط Google/Mapbox**                          | استُبدل بـ Leaflet                           |


---



## 4. فروقات وانحرافات عن الخطة


| الموضوع                          | الخطة               | التنفيذ                      | التقييم                    |
| -------------------------------- | ------------------- | ---------------------------- | -------------------------- |
| **محاكي 3D**                     | جوهر التعليم العملي | غائب بالكامل                 | **انحراف كبير**            |
| **بوابة الدفع**                  | PayTabs/Telr        | دفع يدوي — تأكيد مدير/Admin | قرار عمل مقصود               |
| **الخرائط**                      | Google Maps/Mapbox  | Leaflet + OSM + خريطة سوريا  | انحراف تقني مقبول          |
| **إدخال نتائج المرور**           | المرور فقط          | المرور فقط                   | متوافق                     |
| **عتبة الاختبار التجريبي**       | غير محددة           | 60%                          | قرار تنفيذي                |
| **عتبة الامتحان النظري النهائي** | 60%                 | خارج المنصة — لا تطبيق داخلي | متوافق                     |
| **API-ROUTES.md**                | stubs 501           | خدمات كاملة                  | توثيق قديم                 |
| **إشعارات البريد**               | SendGrid            | nodemailer + SMTP            | تقنياً مختلف، وظيفياً قريب |
| **قفل المحتوى المتدرج**          | متدرج حقيقي         | فرض على API + واجهة          | متوافق                     |
| **ب1/ب2 بعد الاشتراك**           | لا تغيير            | يُثبَّت عند الإنشاء          | متوافق                     |
| **الزائر: أسعار**                | بدون أسعار          | بدون أسعار في العرض العام    | متوافق                     |




### انحرافات من `NOTES.md` (ملاحظات التطوير الحية)

- تبويب «اشتراكي» عند المدير — مُلغى (قرار UI)
- تبويب `/manager` يبقى نشطاً في الشريط — **bug مفتوح**
- حقول الجنس: يُفضّل ذكر/أنثى فقط — قد لا يُطبَّق في كل النماذج

---



## 5. مقارنة بخارطة الطريق (القسم 9)


| المرحلة                                              | الحالة  | ملاحظات                                  |
| ---------------------------------------------------- | ------- | ---------------------------------------- |
| **1 — الأساس** (Auth, رخص, مدارس, Admin تسعير, عينة) | ✅ ~90%  | مكتمل تقريباً                            |
| **2 — الدورات والاشتراك**                            | ✅ ~80%  | ينقص بوابة دفع حقيقية                    |
| **3 — التعليم والتفاعل**                             | ⚠️ ~55% | 3D غائب؛ باقي التعليم جيد                |
| **4 — المرور والنتائج**                              | ✅ ~80%  | تدفق كامل؛ تحسينات توزيع                 |
| **5 — التحسين والنشر**                               | ❌ ~40%  | تقييمات جزئية؛ anti-forgery جزئي؛ لا نشر |


---



## 6. جدول مقارنة سريع: الأدوار × الحالة


| الوظيفة             | زائر | مسجّل | طالب | مدرب  | مدير | Admin | مرور   |
| ------------------- | ---- | ----- | ---- | ----- | ---- | ----- | ------ |
| تصفح رخص/مدارس      | ✅    | ✅     | ✅    | —     | —    | —     | —      |
| عينة مجانية         | ✅    | ✅     | —    | —     | —    | —     | —      |
| اشتراك/دفع          | —    | ✅     | ✅    | —     | —    | —     | —      |
| تعلم نظري/فيديو     | —    | عينة  | ✅    | —     | —    | —     | —      |
| محاكي 3D            | —    | —     | ❌    | ❌     | —    | —     | —      |
| اختبار تجريبي       | —    | —     | ✅    | —     | —    | —     | —      |
| مواعيد عملية        | —    | —     | ✅    | ✅     | ✅    | —     | —      |
| إدارة دورات         | —    | —     | —    | —     | ✅    | —     | —      |
| بنك أسئلة           | —    | —     | —    | تعديل | ✅    | —     | —      |
| رفع أسماء           | —    | —     | —    | —     | ✅    | توزيع | استلام |
| مواعيد/نتائج امتحان | —    | عرض   | عرض  | —     | جزئي | —     | ✅      |
| تسعير/مدارس         | —    | —     | —    | —     | —    | ✅     | —      |


---



## 10. تحديث أمان المستندات ومكافحة التزوير (يوليو 2026)

| المتطلب (الخطة 2.9 / 2.10) | التنفيذ |
|----------------------------|---------|
| تشفير AES للمستندات | `document.service` + `ENCRYPTION_KEY` |
| magic bytes (JPEG/PNG/PDF) | `utils/fileSignature.js` |
| تحميل بمصادقة + سجل وصول | `GET /documents/:id/download` + `DocumentAccessLog` |
| RBAC: طالب/مدير/Admin | `VERIFY_ENROLLMENT_DOCUMENTS`, `VIEW_DOCUMENT_AUDIT` |
| HTTPS في الإنتاج | `httpsRedirect` + Helmet HSTS |
| QR إحصائيات/شهادات/قوائم | `verification.service` + `/verify/*` |
| صفحات تحقق عامة | `/verify/statistics\|certificate\|roster/:token` |
| رفع مستندات (واجهة) | `DocumentUploadSection` — الملف الشخصي + صفحة الاشتراك |
| مستندات إلزامية قبل التقديم | `national_id` + `medical_report` — تحقق في `enrollment.service.create` |
| مراجعة المدير | `GET /documents/enrollment/:id` + زر «مستندات» في `ManagerEnrollmentsPage` |
| اختبارات أمان | `tests/integration/documentSecurity.test.js` + رفض بدون مستندات في `phase1.test.js` |

---


| الميزة | الحالة | ملاحظات |
|--------|--------|---------|
| **Admin فئات الرخص (CRUD)** | ✅ | `/admin/licenses` — minAge, prerequisites, briefDesc, fullDesc, order |
| **Admin الأسئلة الشائعة (CRUD)** | ✅ | `/admin/faq` — سؤال/جواب، ترتيب، تفعيل؛ `GET /faq` للزوار مع fallback |
| **Admin متطلبات التسجيل (CRUD)** | ✅ | `/admin/requirements` — عنوان/وصف/أيقونة/صورة؛ `GET /requirements` للزوار مع fallback |
| **Admin آراء المتعلّمين (CRUD)** | ✅ | `/admin/testimonials` — اسم/اقتباس/تقييم؛ `GET /testimonials` للصفحة الرئيسية مع fallback |
| **تدفق اشتراك 9 مراحل** | ✅ | submitted → under_review → accepted → awaiting_payment → paid → active → completed → exam_pending → final |
| **قائمة انتظار + سعة** | ✅ | maxStudents = مركبات × 5؛ ترقية عند رفض/انتهاء مهلة؛ إشعار waitlist (ليس رفضاً) |
| **إشعارات (بريد فقط)** | ✅ | in-app + email (SMTP/console)؛ SMS معطّل |
| **واجهة الإشعارات** | ✅ | إصلاح `notifications` vs `items`؛ جرس + صفحة + تعليم مقروء |
| **لوحة الطالب** | ✅ | موعد امتحان دائم + انطلاق الدورة + أيام متبقية |
| **job إكمال الدورة** | ✅ | ACTIVE → COMPLETED بعد 15 يوم (cron يومي) |

---

## 8. تحديثات يونيو 2026 (ميزات DriveHub المكتملة)

| الميزة | الحالة | ملاحظات |
|--------|--------|---------|
| **محرر محتوى (مدير)** | ✅ | `/manager/content` — مقالات نظرية؛ بنوك الأسئلة — إضافة MCQ |
| **إحصائيات الطالب** | ✅ | `/student/statistics` — شريط جانبي + رابط من لوحة التحكم |
| **شهادات الطالب** | ✅ | `/student/certificates` — مسار مستقل |
| **اختبارات الأداء** | ✅ | `backend/tests/performance/load-test.js` — `npm run test:perf` |
| **خرائط Google/Mapbox** | ✅ | `VITE_MAP_PROVIDER` + مفاتيح API؛ Leaflet احتياطي |
| **إعلانات عامة** | ✅ | `AdsBanner` على الرئيسية والتذييل — `GET /ads` |

---

## 10. قواعد العمل (Business Rules)

| # | القاعدة | الحالة | التنفيذ |
|---|---------|--------|---------|
| 1 | مدة الدورة 15 يوم — إكمال تلقائي | ✅ | `completeCourses.job` + `COURSE_DURATION_DAYS` |
| 2 | فتح تسجيل دورة جديدة أثناء ACTIVE | ✅ | `course.service.create` — لا قيد |
| 3 | ممنوع إطلاق/رفع قائمة قبل 15 يوم من السابقة | ✅ | `canLaunchNewCourse` + `roster.service.assertRosterTimingAllowed` |
| 4 | حجز المقعد عند تأكيد الدفع فقط | ✅ | `payment.service.confirmByStaff` → `paidCount++` |
| 5 | طلب معلّق واحد لكل مستخدم | ✅ | `assertNoPending` |
| 6 | فلترة طلبات المدير (زمن + مقاعد) | ✅ | `filterRequestsForManager` |
| 7 | إغلاق التسجيل يدوي أو عند الامتلاء | ✅ | `closeRegistration` + `_afterInitialPaid` |
| 8 | 7 أيام افتراضياً بعد الإغلاق؛ مهلة دفع يحددها المدير | ✅ | `launchAfterCloseDays=7`؛ `accept` يستخدم `course.paymentDeadlineDays` |
| 9 | مدة الدرس العملي ساعة | ✅ | `lesson.service` `durationMinutes=60` |
| 10 | فتح المحتوى الكامل فوري بدون موافقة | ✅ | `grantFullContentAccess` عند الدفع/الانطلاق |
| 11 | عمولة المنصة 2% | ✅ | `config.platform.commission` + `splitPayment` |
| 12 | QR + Audit Log للتقارير | ✅ | `verification.service` + `roster.service.verifyByToken` |
| 13 | الاشتراك الرسمي عند الدفع | ✅ | `ENROLLMENT_STATUS.PAID` + `UserRole.STUDENT` |
| 14 | رسوب نهائي نظري → اشتراك جديد كامل | ✅ | `deriveRetakeScope` → `FULL` |
| 15 | رسوب اختبار تجريبي → إعادة فقط | ✅ | `_assertCanTakePractice` يمنع بعد النجاح فقط |
| 16 | لا حذف — إيقاف/حظر | ✅ | `suspendUser` / `suspendSchool`؛ حذف مدرسة بشروط |
| 17 | فئة واحدة لكل طلب | ✅ | حقل `categoryCode` واحد في `enrollment.service.create` |
| 18 | فئة ثانية بعد اجتياز/قبول السابقة | ✅ | `assertPrerequisites` |
| 19 | B1/B2 اختيار مرة واحدة | ✅ | `assertSubTypeLocked` |
| 20 | بيانات الملف محفوظة | ✅ | `profileData` + `assertMinAge` |
| 21 | عينة: زائر جزئي / مسجّل كامل | ✅ | `content.service.getSample` |
| 22 | أرشيف عند إعادة الاشتراك | ✅ | `EnrollmentArchive` |
| 23 | المدرب يقترح — المدير يوافق | ✅ | `TrainingDataEdit` + `QuestionEditRequest` |
| 24 | حساب واحد — أدوار متعددة | ✅ | `UserRole` |
| 25 | تعيين مدرب حسب التخصص | ✅ | `instructor.licenseCategories` + `lesson.service` |
| 26 | تفضيل مدربة | ✅ | `prefersFemaleCoach` + `isFemaleCoach` |
| 27 | اختبار تجريبي مؤقت + بنك + حلول | ✅ | `exam.service` sessions + `review` |
| 28 | إيقاف المدرسة — لا حذف إلا Admin | ✅ | `platform.service.suspendSchool` / `deleteSchool` |
| 29 | تشفير مستندات الهوية | ✅ | `document.service.storeEncrypted` |
| 30 | ترتيب المدارس بالمسافة | ✅ | `school.service.findNearby` + `sortSchoolsByDistance` |

**اختبارات:** `backend/tests/integration/businessRules.test.js`

---

## 7. أولويات مقترحة للإكمال



### أولوية عالية (حرجة للمنتج)

1. **بوابة دفع حقيقية** — PayTabs/Telr + webhooks + تأكيد تلقائي للحجز
2. **تفعيل الإشعارات** — SMTP + SMS للأحداث: قبول، دفع، انطلاق، امتحان
3. **إصلاح قفل المحتوى المتدرج** — ✅ مُنفَّذ (فرض على API)
4. **إزالة/تقييد** `manager/exam-results` — ✅ مُنفَّذ (المرور فقط)
5. **تحديث** `API-ROUTES.md` — يعكس الواقع الحالي



### أولوية متوسطة (قيمة المستخدم)

1. **محاكي 3D** (MVP) — سيناريو واحد + تتبع جلسات
2. **صفحة إحصائيات الطالب** — تقدم، محاولات تجريبية، حضور
3. **عرض الإعلانات** في الرئيسية والبوابات
4. **محرر محتوى** للمدير/Admin — أو أداة seed أفضل
5. **تحسين الحجز التلقائي** — تخصص المدرب + تفضيل مدربة + توفر



### أولوية منخفضة (تحسين وجودة)

1. **QR لكل التقارير** — توسيع نموذج roster
2. **اختبارات E2E** لتدفق الطالب الكامل
3. **اختبارات أداء/حمولة**
4. **إصلاحات UI** من NOTES (تبويب المدير، الجنس)
5. **نشر Production** + مراقبة + HTTPS

---



## 8. الخلاصة

**DriveHub** يحقق **معظم منطق الأعمال** في الخطة v3.3: الأدوار السبعة، تدفق الاشتراك الذكي، قواعد ب1/ب2، إعادة الاشتراك، المرور، التشفير، والأرشيف. الفجوة الأكبر هي **المحاكي ثلاثي الأبعاد** (مذكور في القيمة المضافة والمرحلة 3)، يليه **الدفع والإشعارات الإنتاجية**.

التقدير: **~70–75%** من الخطة الوظيفية منفّذ (Backend أقوى من Frontend في بعض المجالات مثل 3D)، مع **~85%** من البنية التحتية والتدفقات الإدارية.

---

## 11. مقارنة الخطة v3.2

**تاريخ المراجعة:** 4 يوليو 2026  
**المرجع:** DriveHub Project Plan v3.2 — مقارنة منهجية بالكود الحالي

### ملخص تنفيذي بالأقسام

| القسم | النسبة | الحالة |
|-------|--------|--------|
| 0 — مبادئ التصميم وحل التعارض | ~95% | ✅ |
| 1 — نظرة عامة، 7 أدوار، أدوار متعددة | ~95% | ✅ |
| 2.1 — الزائر | ~90% | ✅ |
| 2.2 — المسجّل | ~88% | ✅ |
| 2.3 — الطالب | ~82% | ✅ |
| 2.4 — المدرب | ~85% | ✅ |
| 2.5 — مدير المدرسة | ~90% | ✅ |
| 2.6 — Admin | ~92% | ✅ |
| 2.7 — المرور | ~85% | ✅ |
| 2.9 — أمان المستندات | ~95% | ✅ |
| 2.10 — QR ومكافحة التزوير | ~90% | ✅ |
| 3 — متطلبات غير وظيفية | ~55% | ⚠️ |
| 4 — رحلات المستخدم | ~80% | ✅ |
| 5 — خريطة الموقع (Sitemap) | ~88% | ✅ |
| 6 — تدفقات النظام | ~85% | ✅ |
| قواعد العمل (30 قاعدة) | ~100% | ✅ |
| Batch 3 & 4 (تحديثات) | ~85% | ✅ |
| **الإجمالي الوظيفي** | **~78%** | ⚠️ |

### جدول المتطلبات الرئيسية

| المتطلب | الحالة | ملاحظات |
|---------|--------|---------|
| React + Vite + Tailwind RTL | ✅ | منفّذ |
| JWT + RBAC — 7 أدوار | ✅ | `rolePermissions` + حراس |
| أدوار متعددة + تبديل سياق | ✅ | `RoleSwitcher` |
| لا حذف — إيقاف/حظر فقط | ✅ | |
| بطاقات رخص + FAQ accordion | ✅ | `HomePage`, `FaqPage` |
| أقرب مدارس + خريطة | ✅ | Leaflet / Google / Mapbox |
| متطلبات التسجيل | ✅ | `/requirements` |
| عينة مجانية (زائر جزئي / مسجّل كامل) | ✅ | |
| تدفق اشتراك 9 مراحل | ✅ | |
| مستندات إلزامية (هوية + طبي) | ✅ | قبل التقديم |
| دفع من رصيد المنصة (دفع وهمي) | ✅ | Admin يشحن الرصيد — الطالب يدفع من `/enroll` |
| قائمة انتظار ذكية | ✅ | API + واجهة (موقع في الطلب) |
| حجز مسبق (pre-registration) | ✅ | API + صفحة مدرسة + قائمة في `/enroll` |
| رفض بـ 3 أسباب + بدائل | ✅ | 3 أسباب معيارية + اقتراح مدارس |
| موعد امتحان دائم (لوحة الطالب) | ✅ | `StudentHomePage` |
| محتوى كامل عند الدفع | ✅ | `grantFullContentAccess` |
| اختبار تجريبي 60% | ✅ | |
| QR إحصائيات/شهادات/قوائم | ✅ | `/verify/*` |
| AES + MIME + سجل وصول | ✅ | |
| مدرب يقترح — مدير يوافق | ✅ | `CoachNotesPage` + `ManagerContentEditsPage` |
| بنك أسئلة: مدير يضيف / مدرب يعدّل | ✅ | |
| Admin اعتماد مدارس | ✅ | `AdminCompliancePage` |
| المرور: قوائم + مواعيد + نتائج | ✅ | 4 صفحات |
| إشعارات بريد + داخل التطبيق فقط | ✅ | لا SMS |
| **محاكي 3D** | 🚫 | خارج النطاق |
| **بوابة دفع إلكترونية حقيقية** | 🚫 | رصيد المنصة بدلاً منها |
| **SMS** | 🚫 | بريد إلكتروني فقط |
| نشر Production | ❌ | غير مكتمل |
| اختبارات E2E شاملة | ⚠️ | integration محدود |

### إصلاحات هذه الجلسة (4 يوليو 2026)

| الإصلاح | الملفات |
|---------|---------|
| رصيد المنصة (wallet) — شحن Admin + دفع طالب | `User.js`, `WalletTransaction.js`, `wallet.service.js`, `payment.service.js`, `EnrollPage.jsx`, `AdminUsersPage.jsx` |
| إشعارات خارجية بالبريد فقط (لا SMS) | `notificationChannels.js`, `notification.service.js` |
| قائمة الحجوزات المسبقة + إلغاء في `/enroll` | `EnrollPage.jsx` |
| رابط حجز مسبق عند غياب دورات مفتوحة | `EnrollPage.jsx` |
| عرض موقع قائمة الانتظار في طلبات الطالب | `enrollment.service.js`, `EnrollPage.jsx` |
| 3 أسباب رفض معيارية + نص حر للمدير | `rejectionReasons.js`, `ManagerEnrollmentsPage.jsx` |

### أهم 10 فجوات متبقية (مرتّبة)

1. **محاكي القيادة 3D** — 🚫 خارج النطاق الحالي
2. **بوابة دفع إلكترونية حقيقية** — 🚫 استُبدلت برصيد المنصة (دفع وهمي)
3. **SMS** — 🚫 بريد إلكتروني فقط
4. **تحويل pre-registration → enrollment تلقائي** عند فتح دورة — ⚠️ API موجود، لا job تلقائي
5. **نشر Production** (HTTPS، CI/CD، مراقبة) — ❌
6. **اختبارات E2E** لتدflows كاملة — ⚠️
7. **تحديث API-ROUTES.md** — ⚠️ قديم
8. **إصلاح bug تبويب المدير في الشريط** — ⚠️ UI
9. **توسيع محرر المحتوى** (مقالات + MCQ تفاعلي) — ⚠️ MVP seed
10. **مراقبة أداء المحاكي للمدرب** — 🚫 مرتبط بغياب 3D

---

## تحديث: رفع الصور (يوليو 2026)

- **سياسة:** كل مدخلات الصور عبر رفع ملف (`POST /api/v1/media`) — لا يُقبل `http(s)://` في `imageUrl` / `mediaUrl` / `thumbnailUrl`.
- **خدمة:** `media.service.js` — JPEG/PNG/WebP، تحقق magic bytes، تخزين في `uploads/media/`، عرض عبر `GET /api/v1/media/:id`.
- **واجهات:** `ImageUploadField` — إعلانات Admin، مقالات نظرية Manager، أسئلة بنك الأسئلة.
- **مستندات الهوية/الطبية:** بدون تغيير — `DocumentUploadSection` المشفّر.
- **فيديو YouTube:** `url` للفيديو يبقى رابطاً خارجياً؛ `thumbnailUrl` فقط عبر رفع صورة.
- **اختبارات:** `tests/integration/mediaUpload.test.js`

---

## تحديث: شريط الثقة والصور (يوليو 2026)

- **شريط الثقة (الصفحة الرئيسية):** استبدال marquee المكرر (`[...TRUST_BADGES, ...TRUST_BADGES]`) بسلايدر يدوي يتقدّم كل 3.5 ثانية؛ عند `prefers-reduced-motion` تُعرض كل الشارات دفعة واحدة بدون تكرار.
- **نص الدفع:** «تأكيد دفع يدوي» → «دفع من رصيد المنصة».
- **الصور:** نقل صور الواجهة وبذرة المحتوى (`contentSeed.js`) إلى أصول محلية ثابتة في `frontend/public/images/driving/` بدل روابط Unsplash الخارجية.

---

## تحديث: إدارة فئات الرخص (يوليو 2026)

- **نموذج:** `LicenseCategory` — حقول `requirementsIntro` و`prerequisites` منظّمة `{ label, code?, isRequired, type }` (أنواع: `license` / `basic` / `medical`).
- **إدارة:** `/admin/licenses` — أقسام نظرة عامة، المتطلبات (قائمة ديناميكية)، الحد الأدنى للعمر.
- **عام:** `/licenses/:code` يعرض البيانات من API مع أيقونات حسب نوع المتطلب.
- **توافق:** المتطلبات القديمة بصيغة نص (`['B']`) تُحوَّل تلقائياً عند القراءة والحفظ.
- **اختبارات:** `tests/integration/licenses.test.js`

---

*تم استخراج الخطة من DriveHub Project Plan v3.2 ومقارنتها باستكشاف مباشر للكود.*

---

## تحديث: صفحة امتثال Admin (يوليو 2026)

- إضافة صفحة `/admin/compliance` لمراجعة طلبات الامتثال (طلب إضافة مدرسة) من لوحة الإدارة.
- دعم API جديد: عرض قائمة الطلبات، عرض التفاصيل الكاملة، قبول الطلب، رفض الطلب مع سبب.
- التفاصيل تشمل: بيانات المدرسة، بيانات المالك، الحساب البنكي، والمرفقات إن وجدت.
- بعد القبول/الرفض يختفي الطلب تلقائياً من قائمة المعلّق، مع خيار إظهار السجل.

---

## تحديث: نسيت كلمة المرور (يوليو 2026)

- **APIs جديدة (Auth):**
  - `POST /api/v1/auth/forgot-password` — يستقبل `email` ويعيد رسالة نجاح عامة دائماً لمنع كشف وجود البريد.
  - `POST /api/v1/auth/verify-reset-code` — يستقبل `email + code` للتحقق من OTP (6 أرقام).
  - `POST /api/v1/auth/reset-password` — يستقبل `email + newPassword` مع `resetToken` (أو `code` كخيار بديل).
- **الأمان:**
  - تخزين رمز OTP بصيغة hash فقط (SHA-256) في `PasswordResetToken`.
  - صلاحية OTP افتراضياً 10 دقائق (`PASSWORD_RESET_CODE_TTL_MINUTES`).
  - حد محاولات التحقق افتراضياً 5 (`PASSWORD_RESET_MAX_ATTEMPTS`).
  - Rate limit على طلب الرمز والتحقق منه.
  - عند إعادة تعيين كلمة المرور: إبطال كل `RefreshToken` النشطة للمستخدم.
- **الواجهة (RTL عربي):**
  - رابط **«نسيت كلمة المرور؟»** في صفحة تسجيل الدخول.
  - صفحة `/forgot-password` بثلاث خطوات: طلب الرمز → التحقق → تعيين كلمة مرور جديدة.
  - دعم إعادة إرسال الرمز مع عداد زمني.

### إعداد البريد الإلكتروني / Email setup (no custom domain)

**بدون دومين خاص؟** استخدم **Mailtrap Sandbox** للتطوير (أي مستلم → صندوق Mailtrap) أو **Gmail SMTP** للإرسال الحقيقي (من `@gmail.com` إلى أي بريد).

**No custom domain?** Use **Mailtrap Sandbox** for dev (any recipient → Mailtrap inbox) or **Gmail SMTP** for real delivery (from `@gmail.com` to any inbox).

الكود في `notificationChannels.js` — **أولوية النقل / transport priority:**

1. `MAILTRAP_USE_SANDBOX=true` + `MAILTRAP_API_TOKEN` + `MAILTRAP_INBOX_ID` → `MailtrapClient` sandbox API
2. `MAILTRAP_API_TOKEN` فقط (بدون sandbox) → Email Sending API (`hello@demomailtrap.co` → **مالك الحساب فقط**)
3. `SMTP_HOST` → nodemailer (Gmail أو Sandbox SMTP)
4. لا شيء → طباعة في console (dev)

#### الخيار A — Mailtrap Sandbox API (موصى به للتطوير)

```env
MAILTRAP_API_TOKEN=<من https://mailtrap.io/api-tokens>
MAILTRAP_USE_SANDBOX=true
MAILTRAP_INBOX_ID=2564102
MAIL_FROM=sandbox@example.com
MAIL_FROM_NAME=DriveHub
```

**كيف تحصل على `MAILTRAP_INBOX_ID`:** افتح صندوق Sandbox في Mailtrap وانسخ الرقم من الرابط:
`https://mailtrap.io/inboxes/2564102` → المعرف هو **2564102**

أي بريد مستلم (`student@anywhere.com`) يُلتقط في صندوق Mailtrap — لا حاجة لدومين.

#### الخيار B — Mailtrap Sandbox SMTP (بديل)

```env
MAILTRAP_USE_SANDBOX=false
MAILTRAP_API_TOKEN=
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=<من Inbox → SMTP Settings>
SMTP_PASS=<من Inbox → SMTP Settings>
MAIL_FROM=sandbox@example.com
MAIL_FROM_NAME=DriveHub
```

#### الخيار C — Gmail SMTP (إرسال حقيقي بدون دومين)

```env
MAILTRAP_API_TOKEN=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your@gmail.com
SMTP_PASS=xxxx xxxx xxxx xxxx
SMTP_FROM="DriveHub <your@gmail.com>"
```

كلمة مرور التطبيق (16 حرف): [Google App Passwords](https://myaccount.google.com/apppasswords)

#### الخيار D — Mailtrap Email Sending (إنتاج، يحتاج دومين موثّق)

```env
MAILTRAP_API_TOKEN=<sending token>
MAILTRAP_USE_SANDBOX=false
MAIL_FROM=hello@your-verified-domain.com
```

**اختبار forgot-password يدوياً:**
1. اختر أحد الخيارات أعلاه في `backend/.env` وأعد تشغيل الخادم.
2. `POST /api/v1/auth/forgot-password` مع `{ "email": "<مستخدم موجود>" }` — الاستجابة دائماً `200` عامة.
3. احصل على الرمز من صندوق Mailtrap Sandbox، أو من console الخادم في dev.

> **لا ترفع `.env` إلى Git** — استخدم `.env.example` كمرجع فقط.

---

## تحديث: مصفوفة فئات الرخص (يوليو 2026)

- تم توحيد مصفوفة الرخص المعتمدة لفئات: `B`, `C`, `D1`, `D2`, `A`, `H`, `W` عبر seed وواجهات العرض والإدارة.
- كل فئة أصبحت تحتوي نصوص عربية محدثة ضمن: `briefDesc`, `fullDesc`, `requirementsIntro`, `vehicleTypes` بالإضافة إلى `minAge` و`prerequisites`.
- التسلسل المعتمد: `C` يتطلب `B` (3 سنوات)، `D1` يتطلب `C` (سنتين)، `D2` يتطلب `D1` (سنتين)، مع متطلبات خاصة لفئات `A/H/W`.