VITE_API_URL=/api/v1

# Maps — leaflet (default) | google | mapbox
# Falls back to Leaflet/OpenStreetMap when provider is set but key/token is missing.
VITE_MAP_PROVIDER=leaflet
VITE_GOOGLE_MAPS_KEY=
VITE_MAPBOX_TOKEN=
