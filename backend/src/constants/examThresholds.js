/** عتبة النجاح للاختبار التجريبي (داخل المنصة) */
const PRACTICE_PASS_THRESHOLD = 60;

/** عتبة النجاح للامتحان النهائي النظري (خارج المنصة — للمرجعية فقط) */
const FINAL_THEORY_PASS_THRESHOLD = 60;

module.exports = {
    PRACTICE_PASS_THRESHOLD,
    FINAL_THEORY_PASS_THRESHOLD,
};
